import { PhaseStub } from "@/components/phase-stub";

export const metadata = { title: "Courses" };

export default function CataloguePage() {
  return (
    <PhaseStub
      eyebrow="F1 · Public catalogue"
      title="Course catalogue"
      description="Searchable course cards will live here (WHO Academy pattern). Built in Phase 2."
      requirement="F1 — Public landing page and course catalogue"
    />
  );
}
